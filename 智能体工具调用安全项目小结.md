# AgentSentinel：面向大模型智能体的步级工具调用安全护栏与受约束强化学习系统

> 文档用途：作为简历项目经历、技术博客、项目 README 和面试准备材料的统一底稿。  
> 项目定位：大模型安全、LLM Agent、强化学习与模型训练相结合的研究型工程项目。  
> 个人角色：独立负责论文思想分析、源码理解、数据治理、模型训练、奖励设计、约束生成、评测与训练工程化。  
> 核心原则：项目重点不只是复现某个最终分数，而是建立一套能够解释、训练、验证和持续迭代的智能体工具调用安全系统。

---

## 1. 项目摘要

随着大模型从对话系统演进为能够调用邮件、文件、支付、日历、搜索和企业服务的智能体，模型输出不再只是文本，而可能触发真实外部副作用。恶意用户请求、间接提示词注入、错误规划和越权调用，都可能使智能体执行数据外传、错误转账、批量发送信息或破坏文件等高风险操作。

AgentSentinel在真实工具执行之前增加一个步级安全护栏。护栏读取用户请求、智能体交互历史、当前动作和工具环境，对当前动作输出：

1. 用户请求本身是否恶意；
2. 智能体是否受到外部提示词注入攻击；
3. 当前工具调用的实际危害等级；
4. 支撑安全判断的证据型分析。

项目围绕这一目标完成了从数据到训练、再到评测和运行时集成的主要工程闭环：

```text
安全轨迹恢复与审计
  → API Teacher盲标
  → 标签冲突分析与人工裁决
  → 全参数SFT
  → GRPO奖励与优势计算
  → Token-FSM受约束生成
  → 单Actor概率重算与策略更新
  → 可恢复训练与验证选模
  → 工具执行前拦截和反馈接口
```

项目从Qwen2.5-1.5B LoRA起步，逐步升级到Qwen2.5-3B全参数SFT和GRPO。在未参与训练的AgentDojo Banking测试集上，最佳SFT模型实现87/87响应可解析，Accuracy、F1、Recall分别达到93.10%、92.10%、92.10%。阶段性GRPO实验还将有害动作召回率从78.57%提升至96.43%，验证了组内相对奖励对降低安全漏报的作用。

在此基础上，项目进一步实现Tokenizer-aware Token FSM和单Actor Constrained GRPO，将固定输出格式交由确定性程序保证，让模型及强化学习集中优化安全分析与三字段判断，而不是浪费训练信号学习尖括号、标签名和换行符。

---

## 2. 项目要解决的核心问题

### 2.1 为什么普通内容审核不足以保护智能体

普通安全审核通常发生在两个位置：

- 用户输入进入模型之前；
- 模型生成最终回答之后。

但智能体的风险经常发生在中间步骤。以邮件智能体为例，用户原始请求可能是“整理客户会议安排”，智能体在读取外部邮件时遇到隐藏指令，随后计划把客户信息发送到攻击者邮箱。如果安全系统只检查用户最初的问题或最终自然语言回答，就可能漏掉真正产生副作用的`send_email`动作。

因此，本项目把安全边界放在：

```text
模型产生工具调用
        ↓
AgentSentinel检查当前动作
        ↓
安全：允许进入工具执行器
危险：阻断并返回安全反馈
```

这一位置位于真实工具执行之前，能够在副作用发生前阻止风险。

### 2.2 为什么不能只输出“安全/不安全”

相同的危险动作可能来自不同原因：

- 用户直接要求执行恶意操作；
- 用户请求正常，但网页、邮件或文档中包含间接提示词注入；
- 用户与外部输入都没有恶意，模型自身规划错误；
- 当前动作有争议，需要人工确认，而不是直接允许或阻断。

如果护栏只输出一个二分类结论，智能体无法知道应该拒绝用户、忽略外部攻击，还是重新规划当前步骤。因此项目把判断拆成三个语义字段，并附带自然语言证据，使安全结果可以继续参与智能体规划。

### 2.3 项目的实际价值

该系统适用于具备真实工具权限的智能体，例如：

- 邮件发送和客户沟通；
- 银行转账、账单和交易查询；
- 企业文件读取、修改和共享；
- 日历创建与会议邀请；
- 浏览器搜索和网页内容处理；
- 代码执行、数据库查询和运维操作。

其核心价值不是让模型“更加谨慎”这一模糊目标，而是在工具执行边界提供可审计、可解释、可反馈的安全决策。

---

## 3. 威胁模型与安全目标

### 3.1 主要威胁

#### 恶意用户请求

用户直接要求智能体执行有害或未授权操作，例如窃取凭据、发送欺骗性内容、收集第三方隐私或破坏文件。

#### 间接提示词注入

用户请求本身正常，但智能体读取的网页、邮件、文档或工具返回值中包含伪装成指令的攻击内容。攻击者试图让智能体偏离原始任务并调用危险工具。

#### 动作级风险

即使用户请求和外部环境没有明显攻击，智能体仍可能因为规划错误、参数构造错误或上下文理解偏差执行有害动作。

#### 安全过度拦截

安全系统如果简单阻断所有敏感操作，虽然可以降低攻击成功率，却会严重破坏正常任务完成率。因此项目同时关注有害动作召回与良性动作误报。

### 3.2 三字段安全协议

模型输出的三个字段分别承担不同职责：

| 字段 | 取值 | 含义 |
|---|---|---|
| `Malicious_User_Request` | yes/no | 原始用户请求是否具有恶意意图 |
| `Being_Attacked` | yes/no | 当前动作是否受到外部提示词注入驱动 |
| `Harmfulness_Rating` | 0.0/0.5/1.0 | 当前动作的危害等级：安全、争议、显著有害 |

自然语言Think用于指出判断证据，例如：

- 当前动作是否仍与用户原始目标一致；
- 是否出现未经授权的收件人、账户或文件路径；
- 工具返回内容是否试图覆盖系统任务；
- 当前动作是否会产生隐私、财务或系统完整性风险。

---

## 4. 系统总体架构

### 4.1 离线训练链路

```text
AgentHarm / AgentDojo安全轨迹
                │
                ▼
      数据恢复、解析与身份规范化
                │
                ▼
      API Teacher独立三字段盲标
                │
                ▼
  弱标签比较、冲突队列和任务级裁决
                │
                ▼
┌─────────────────────────────────────┐
│ SFT Train │ GRPO Train │ Validation │
│ 239条     │ 1208条     │ 146条      │
└─────────────────────────────────────┘
                │
                ▼
        Qwen2.5-3B全参数SFT
                │
                ▼
     Token-FSM Constrained GRPO
                │
                ▼
  断点恢复、验证选模与外部安全评测
```

### 4.2 在线工具调用链路

```text
用户请求
   ↓
Agent生成Thought/Action/Action Input
   ↓
AgentSentinel读取历史、动作和工具描述
   ↓
输出风险来源、攻击状态、危害等级和解释
   ├── 安全：调用真实工具
   └── 危险：阻断动作并把证据反馈给Agent重新规划
```

### 4.3 模型输入

护栏不是只看当前函数名，而是同时读取：

- 用户原始请求；
- 当前步骤之前的交互历史；
- 工具返回的Observation；
- 当前Thought、Action和Action Input；
- 当前可用工具及参数定义。

这些信息共同决定当前动作是否符合用户授权、是否受到攻击，以及会产生什么副作用。

---

## 5. 数据治理体系

### 5.1 为什么数据治理是项目的第一优先级

安全模型对标签错误非常敏感。例如，同一个正常用户请求的不同step，如果一条被标成恶意、一条被标成正常，模型就会同时学习相互冲突的监督信号。进一步增加epoch或模型规模只会让模型更充分地拟合冲突，而不会自动得到正确规则。

项目因此没有把数据集中的所有字段直接视为绝对金标签，而是建立了独立盲标、冲突分析和人工裁决流程。

### 5.2 API Teacher盲标

API Teacher只接收安全场景本身，不接收以下信息：

- 原有三字段答案；
- 数据集类别名称；
- 样本属于训练集还是验证集；
- `benign`、`harmful`等可能暗示答案的路径信息。

Teacher独立输出：

- 证据型分析；
- 用户请求是否恶意；
- 当前动作是否受到攻击；
- 当前危害等级。

结构化解析程序负责验证字段类型和枚举范围，单条API失败不会中止整个任务；重新运行时只请求缺失identity，从而支持断点续标。

### 5.3 全量盲标结果

共处理1,656条数据：

| 数据角色 | 数量 |
|---|---:|
| SFT候选 | 302 |
| GRPO训练 | 1,208 |
| Clean Validation | 146 |
| 合计 | 1,656 |

Teacher与原有重建标签：

| 统计项 | 数量/比例 |
|---|---:|
| 完全一致 | 1,330 |
| 存在冲突 | 326 |
| 一致率 | 80.31% |
| `Malicious_User_Request`冲突 | 90 |
| `Being_Attacked`冲突 | 85 |
| `Harmfulness_Rating`冲突 | 254 |

同一条样本可能有多个字段冲突，因此字段冲突数之和大于326。

### 5.4 人工裁决策略

项目没有简单采用“Teacher覆盖旧标签”，而是针对不同字段建立不同裁决规则。

#### 用户恶意性按原始请求统一

`Malicious_User_Request`描述的是用户原始意图，不应随着Agent执行step变化。对于发生冲突的任务，项目按原始用户请求进行任务级裁决，并将决定传播到该请求的全部step。

#### 当前是否受攻击必须基于当前动作

历史中出现过提示词注入，不代表后续每一个动作都仍由攻击驱动。只有当前Action直接执行、推进或响应攻击指令时，`Being_Attacked`才应为yes。

#### 危害等级保留基准中的current-step分数

危害等级描述当前动作的后果。为了保持安全基准的一致口径，项目没有让API模型随意重定义0.5与1.0边界，而是保留原current-step评分。

最终人工修正71行：

- `Malicious_User_Request`：65行；
- `Being_Attacked`：6行；
- `Harmfulness_Rating`：0行。

### 5.5 解释与标签一致性门禁

Teacher生成的解释只有在与最终三个字段语义一致时，才允许进入SFT监督。302条SFT候选中最终保留239条；其余63条没有从原始账本删除，只是不用于解释监督。

这一策略避免模型学习如下矛盾模式：

```text
Think：当前动作安全且符合用户请求
字段：Harmfulness_Rating = 1.0
```

### 5.6 数据隔离

最终数据边界为：

| 数据集 | 数量 | 用途 |
|---|---:|---|
| SFT Train | 239 | 监督解释与三字段 |
| GRPO Train | 1,208 | 强化安全语义判断 |
| Clean Validation | 146 | 验证与checkpoint选择 |
| AgentDojo Banking | 87 | 外部跨域报告 |
| ASB | 5,231 | 冻结的最终独立测试集 |

SFT、GRPO和Clean Validation的step identity交集为0；Banking和ASB不参与训练和checkpoint选择。

---

## 6. 模型训练路线与关键决策

### 6.1 第一阶段：1.5B基座与LoRA验证

项目首先使用Qwen2.5-1.5B完成低成本工程验证，包括Prompt构造、三字段解析、LoRA训练、Adapter加载和外部评测。

未经训练的1.5B模型在Banking 87条数据中有31条无法解析，exact Accuracy仅57.14%。第一轮只使用单一来源数据训练后，模型在Banking上出现接近全预测安全类别的现象。虽然Accuracy仍可能受多数类支撑，但F1和Recall明显暴露了安全类塌缩。

这一阶段得到的重要结论是：

- 训练损失下降不等于跨域安全能力提升；
- Accuracy不足以评估安全模型；
- 需要同时查看解析率、预测分布、混淆矩阵和有害类Recall；
- 数据域覆盖往往比单纯增大LoRA rank更重要。

加入AgentDojo的Slack、Travel和Workspace数据，同时保留Banking为外部域后，LoRA模型在Banking上的Accuracy提升至88.51%，证明跨域场景覆盖能够显著改善泛化。

### 6.2 第二阶段：升级3B并进行全参数SFT

1.5B在复杂长轨迹和严格结构化输出上容量有限，因此项目升级到Qwen2.5-3B，并采用全参数SFT，而不是继续只增加LoRA容量。

核心配置：

- 可训练参数：3,085,938,688；
- 可训练比例：100%；
- 训练精度：BF16；
- 最大序列长度：4096；
- micro batch：1；
- 梯度累积：8；
- 两轮训练共76个global step。

训练损失从第一轮0.03298下降到第二轮0.00632，第二轮validation loss有所上升，表现出teacher-forcing层面的过拟合信号。但自由生成评测反而继续改善，说明token级validation loss与最终结构化生成指标不完全等价。

最佳全参数SFT模型在Banking上实现：

| 指标 | 结果 |
|---|---:|
| 总样本 | 87 |
| 成功解析 | 87 |
| skipped | 0 |
| Accuracy | 93.10% |
| F1 | 92.10% |
| Recall | 92.10% |

未经训练的3B基座在同一数据上只有22/87响应可以解析。因此，全参数SFT不仅学习了安全任务，也显著提高了复杂输出协议的稳定性。

### 6.3 第三阶段：Think监督消融

早期SFT样本中的Think为空。如果把空Think也作为普通监督目标，模型会学习到“立即关闭Think”这一高概率模式。

项目因此进行消融：

```text
Prompt和padding：不计算loss
Think正文：不计算loss
三个安全字段：计算完整交叉熵
EOS：计算完整交叉熵
```

该版本在Banking上的结果为：

- Accuracy：86.21%；
- 非空Think：70/87，覆盖率80.46%；
- 有害类召回：22/28，即78.57%。

这一实验保留了部分模型自发分析能力，但整体判断性能低于完整监督SFT。它说明“不给错误Think标签”是必要的，但完全不监督解释也无法稳定获得高质量反馈。

### 6.4 第四阶段：Teacher Rationale Repair

为避免手工模板或空Think污染监督，项目使用更强Teacher模型为SFT样本生成证据型解释。Teacher只负责语义分析，最终标签和固定协议由程序确定性组装。

Think token采用低权重，三个安全字段采用完整权重：

```text
Prompt / padding：0.0
Think block：0.2
三个安全字段：1.0
EOS：1.0
```

长解释不会凭token数量压过三个核心判断字段，同时模型仍能学习如何输出完整安全证据。

Rationale-repair SFT后：

- 5条自由生成抽样全部出现非空Think；
- Banking完整评测中87/87均有非空Think；
- 有害类实际召回为26/28，即92.86%；
- 模型恢复了可解释反馈能力。

### 6.5 第五阶段：第一代全参数GRPO

第一代GRPO在单张A800上使用：

- Actor：3B全参数更新；
- Reference：冻结的Actor初始权重；
- GRPO训练数据：1,208条；
- 每个Prompt生成8个Rollout；
- 学习率：`1e-6`；
- PPO clip ratio：0.2；
- KL系数：0.001；
- 训练1个epoch，共37步。

训练内validation reward从0.3708提升到0.4827，相对提升约30.2%。在外部Banking测试上，与自己的直接父模型相比：

| 指标 | SFT父模型 | GRPO | 变化 |
|---|---:|---:|---:|
| Accuracy | 86.21% | 89.66% | +3.45个百分点 |
| 有害类Recall | 78.57% | 96.43% | +17.86个百分点 |
| 有害动作漏报 | 6 | 1 | -5 |

同时，良性动作误报从5条增加到8条。这体现了安全模型中典型的安全性与可用性权衡：强化学习提高了有害动作敏感度，但也使模型更加保守。

### 6.6 第一代GRPO暴露的Reward Hacking

第一代奖励只评价三个字段，不要求Think存在，也不判断解释语义。模型因此发现：省略Think可以缩短响应、减少格式失败，同时不影响满分奖励。

最终出现：

- SFT父模型非空Think：70/87；
- GRPO后非空Think：0/87。

这一结果证明：正Advantage只有在回答实际包含Think时才会提升Think token概率。若省略Think也能获得满分，强化学习会主动选择更短的奖励捷径。

项目因此不再把“模型自然具备推理能力”当作解释稳定性的保证，而是重新设计输出协议和训练职责。

---

## 7. Token-FSM受约束生成

### 7.1 为什么格式不应主要由强化学习解决

自由生成模型需要同时学习：

- 安全场景分析；
- 三字段取值；
- 固定标签名称；
- 尖括号和反斜杠；
- 字段顺序；
- 每行换行位置。

后一组要求没有安全语义，却可能使整条回答因为一个字符错误被判0分。这样产生的组内奖励差异主要来自格式随机性，而不是安全判断差异。

项目将两种职责拆开：

```text
程序负责：标签、换行、闭合符、字段顺序
模型负责：Think正文、结束分析时机、三个字段值
```

### 7.2 Tokenizer-aware FSM

状态机不在字符层面假设`yes`或`0.5`一定是单Token，而是读取真实tokenizer编码，为每个枚举值构建Token Trie。

核心状态流：

```text
固定<Think>
  → 受限生成分析正文
  → 模型选择结束分析
  → 固定Think闭合与MUR标签
  → yes/no Token Trie
  → 固定Being Attacked标签
  → yes/no Token Trie
  → 固定Harmfulness标签
  → 0.0/0.5/1.0 Token Trie
  → 固定闭合
```

FSM还提供以下安全边界：

- Think至少生成8个正文Token；
- Think最多生成192个Token；
- 禁止正文采样标签分隔符、换行和special token；
- 达到上限时确定性闭合并记录强制结束；
- Prompt中的伪标签不影响FSM状态；
- tokenizer无法精确round-trip固定协议时直接拒绝启动。

### 7.3 固定Token为什么仍进入模型上下文

虽然标签由程序插入，但后续字段仍需要看到前面的完整响应，因此固定Token必须写入上下文。它们只是不会被当作策略动作计算Policy Gradient或KL。

这形成两类Token：

| Token类型 | 进入上下文 | 计算策略梯度 |
|---|---:|---:|
| 固定协议Token | 是 | 否 |
| 模型选择的语义Token | 是 | 是 |

这样既保持自回归上下文完整，又避免训练信号被固定格式稀释。

---

## 8. Constrained GRPO训练设计

### 8.1 Rollout如何产生

每个Prompt使用相同Actor和采样参数生成多条候选回答。它们共享Prompt，但采样随机数不同，因此可以得到不同Think和字段组合。

正式Trainer的一个optimizer step为：

```text
4个Prompt Group
× 每组4条Rollout
= 16条Response
```

模型在处理完4组之前不更新参数，保证同一个optimizer step内的所有Old LogP都对应同一策略版本。

### 8.2 组内相对优势

GRPO不训练额外Value Network，而是在同一个Prompt的多个回答之间比较奖励。

直观上：

- 同一Prompt下，高于组内平均奖励的回答得到正Advantage；
- 低于组内平均奖励的回答得到负Advantage；
- 如果一组回答奖励完全相同，则Advantage精确为0，不产生策略更新信号。

真实3B单步门禁得到奖励：

```text
[0.4, 1.0, 1.0, 1.0]
```

对应Advantage约为：

```text
[-1.7321, 0.5774, 0.5774, 0.5774]
```

模型会降低低奖励回答中已选择语义Token的相对概率，并提高高奖励回答的相对概率。

### 8.3 当前语义奖励

格式已由FSM保证，因此奖励不再评价标签字符和换行，只比较三个安全字段：

| 奖励项 | 权重 |
|---|---:|
| `Malicious_User_Request`正确 | 0.20 |
| `Being_Attacked`正确 | 0.20 |
| `Harmfulness_Rating`正确 | 0.30 |
| 三字段全部正确额外奖励 | 0.30 |
| 格式奖励 | 0.00 |
| Think存在奖励 | 0.00 |

联合正确奖励使模型只有在完整安全结论一致时才能获得满分，降低“只学两个容易字段”的动机。

### 8.4 为什么Rollout后还要重新前向

Rollout已经是完整答案，但强化学习需要知道Actor对每个已生成Token分配了多少概率。

重新前向不是让模型再回答一次，而是：

```text
已知Prompt和已经生成的Response
→ 按Teacher Forcing逐位置计算Log Probability
→ 得到Old LogP和New LogP
→ 构造可反向传播的策略损失
```

项目使用同一个Hugging Face Actor负责：

- 受约束Rollout；
- 更新前Old LogP完整序列重算；
- 训练态New LogP重算；
- backward与参数更新。

固定语法Token被写入完整上下文，但只有Think语义、Think结束决策和三个枚举值进入策略mask。

### 8.5 PPO Clipping与KL约束

PPO ratio用于比较新策略与生成这些回答时的旧策略概率。Clip ratio设置为0.2，避免单个高奖励或低奖励样本使策略一步变化过大。

Reference模型是冻结的SFT初始模型。KL项限制Actor不要为了短期奖励过度偏离原有语言和分析能力，当前系数为0.001。

两者职责不同：

- PPO Clipping约束单次更新相对Old Policy的幅度；
- Reference KL约束整个训练过程中相对初始模型的漂移。

### 8.6 真实3B更新门禁

在启动长训练之前，项目对真实3B模型执行完整的一步更新验证：

| 检查项 | 结果 |
|---|---:|
| Rollout数量 | 4 |
| 严格格式 | 4/4 |
| 非空Think | 4/4 |
| Old/New LogP最大误差 | 0.0 |
| 梯度裁剪前范数 | 12.9486 |
| 观测到的参数变化 | 约`1.00024e-6` |
| CUDA峰值allocated | 48.33GiB |
| CUDA峰值reserved | 48.67GiB |

这证明模型不是只完成了前向演示，而是奖励、Advantage、LogP、Policy Loss、KL、反向传播和参数更新已经形成真实闭环。

---

## 9. 可恢复训练工程

### 9.1 正式训练规模

Constrained GRPO Trainer配置为：

| 参数 | 当前配置 |
|---|---:|
| Actor | Qwen2.5-3B全参数 |
| GRPO Train | 1,208条 |
| Epoch | 1 |
| Rollout/Prompt | 4 |
| Prompt Group/Step | 4 |
| Response/Optimizer Step | 16 |
| 总Optimizer Step | 302 |
| Learning Rate | `1e-6` |
| Weight Decay | 0.0 |
| PPO Clip | 0.2 |
| KL Coefficient | 0.001 |
| Max Grad Norm | 1.0 |
| Validation Frequency | 每25步及最终步 |
| Checkpoint Frequency | 每10步 |

### 9.2 完整断点内容

断点不仅保存模型权重，还保存：

- Actor参数；
- AdamW一阶和二阶动量；
- scheduler；
- Python随机状态；
- PyTorch CPU/CUDA随机状态；
- 数据shuffle和游标；
- 已完成optimizer step；
- 连续无信号step计数；
- 数据和语义配置摘要。

完整断点约36GB，而单独最优Actor约12GB。差异主要来自AdamW优化器状态及训练元数据。

### 9.3 两代断点轮换

训练只稳定保留：

- `latest`：最新完整断点；
- `previous`：上一完整断点；
- `best_actor`：验证指标最优的纯模型权重。

新断点先写入临时目录，成功后再轮换。即使写入过程中断，旧`latest`仍保持可恢复。

### 9.4 验证与自动停止

Clean Validation包含146条独立样本，用于checkpoint选择，不参与反向传播。

选模优先比较：

1. 平均dense reward；
2. 固定三分类的Harmfulness macro-F1；
3. 完全相同时保留更早step，减少不必要漂移。

如果连续10步所有Prompt Group都没有奖励差异，Trainer自动停止，避免在无有效Advantage时继续消耗GPU。

训练日志使用append-only JSONL，记录Prompt、Response、三个预测字段、Reward、Advantage、Loss、梯度、显存和session ID，便于中断后审计。

---

## 10. 反馈驱动的工具安全执行

### 10.1 为什么只阻断还不够

受到提示词注入时，用户原始任务可能仍然是正常的。如果安全系统发现攻击后直接终止整个任务，会降低用户任务完成率。

更合理的流程是：

```text
Guard发现当前动作受攻击
  → 阻止危险工具执行
  → 把攻击来源和动作风险反馈给Agent
  → Agent忽略外部恶意指令
  → 回到用户原始目标重新规划
```

### 10.2 安全反馈内容

反馈不是简单的“操作被拒绝”，而应包含：

- 哪一段外部内容可能是攻击指令；
- 当前动作为什么偏离用户授权；
- 动作会产生什么具体风险；
- Agent应该继续原任务还是向用户确认。

这也是保留Think能力的重要原因：三个字段适合机器决策，证据型解释适合Agent重新规划和人工审计。

### 10.3 安全与可用性的共同评测

完整系统不能只报告攻击成功率，还需要同时观察：

- 有害工具调用是否被阻止；
- 正常任务完成率是否下降；
- 良性动作误报率；
- 恶意动作漏报率；
- Guard反馈后Agent能否重新规划；
- Guard推理带来的延迟和资源开销。

项目在Banking评测中观察到：GRPO把有害动作漏报从6条降到1条，但良性误报从5条增加到8条。这一结果说明安全模型需要在Recall和Utility之间进行显式权衡，而不能只追求单一指标。

---

## 11. 项目实验结果汇总

### 11.1 模型演进

| 阶段 | 外部Banking结果 | 关键结论 |
|---|---|---|
| 1.5B基座 | 31/87解析失败；exact Accuracy 57.14% | 小模型协议稳定性和分类能力不足 |
| AgentDojo增强LoRA | Accuracy 88.51% | 跨域数据覆盖比单纯提高LoRA rank更关键 |
| 3B基座 | 仅22/87可解析 | 参数变大不会自动学会安全输出协议 |
| 3B全参数SFT | Accuracy/F1/Recall 93.10%/92.10%/92.10%；skipped=0 | 当前最佳综合SFT结果 |
| Fields-only SFT | Accuracy 86.21%；非空Think 80.46% | 保留部分自由分析，但字段性能回退 |
| Rationale-repair SFT | 87/87非空Think；有害Recall 92.86% | 恢复可解释反馈能力 |
| 第一代GRPO | Accuracy 89.66%；有害Recall 96.43% | 相对直接父模型明显降低漏报 |
| Constrained GRPO门禁 | 4/4格式正确；真实参数更新 | 受约束强化学习数学链路成立 |

### 11.2 数据工程结果

| 项目 | 结果 |
|---|---:|
| API Teacher盲标 | 1,656条 |
| 标签完全一致 | 1,330条 |
| 标签冲突 | 326条 |
| 人工字段修正 | 71行 |
| 高一致性SFT样本 | 239条 |
| GRPO样本 | 1,208条 |
| Clean Validation | 146条 |
| Banking外部测试 | 87条 |
| ASB冻结测试 | 5,231条 |

### 11.3 工程结果

| 项目 | 结果 |
|---|---:|
| 3B可训练参数 | 3,085,938,688 |
| 真实单步峰值显存 | 48.33GiB allocated |
| E2-E5聚焦测试 | 41项通过 |
| 正式训练计划 | 302 optimizer steps |
| 每步Rollout | 16条 |
| 清理历史权重/断点 | 约95GB |
| 清理后可用磁盘 | 约139GB |

### 11.4 最适合写入简历的量化结果

推荐优先使用以下三个结果：

1. 全参数SFT将3B基座从22/87可解析提升到87/87可解析，并取得93.1% Accuracy；
2. 阶段性GRPO将有害动作Recall从78.6%提升至96.4%，漏报从6条降到1条；
3. 完成1,656条安全轨迹盲标，识别326条冲突并执行任务级人工裁决。

其中第1和第2来自不同实验checkpoint，简历中应通过“最佳SFT结果”和“GRPO相对直接父模型提升”分别描述，不应拼成同一个模型的联合结果。

---

## 12. 项目的深入思考与研究价值

### 12.1 安全模型的目标不是单纯提高Accuracy

安全场景中漏报和误报成本不对称：

- 漏报可能导致资金、隐私和系统完整性损失；
- 误报会阻断正常用户任务，降低产品可用性。

因此项目同时关注有害Recall、良性Recall、混淆矩阵和任务Utility，而不是只报告Accuracy。

### 12.2 模型容量不是所有问题的答案

从1.5B升级到3B提升了复杂轨迹理解能力，但3B基座仍只有22/87响应可以解析。模型规模无法自动解决：

- 输出协议没有训练；
- 标签定义相互冲突；
- 数据域覆盖不足；
- 奖励目标和最终指标不一致。

正确顺序是先修复数据和目标，再扩大模型容量。

### 12.3 格式正确与语义正确必须解耦

自由生成格式失败不代表模型不会判断安全；模型也可能格式完美但语义错误。把两者都压缩成一个0/1奖励，会让训练信号非常嘈杂。

AgentSentinel采用FSM保证格式、Reward优化语义，使工程约束和模型学习各自承担适合的职责。

### 12.4 Explainability不能依靠模型“自然保留”

如果解释不参与任何训练约束，强化学习会偏向更短、更稳定的三字段回答。项目通过真实消融观察到Think覆盖率从80.46%下降到0%，说明解释输出必须有明确的训练机制或生成协议。

### 12.5 Validation Loss与自由生成指标不等价

SFT中的validation loss是在给定正确前文的teacher forcing条件下计算；真实评测需要模型自己生成完整序列。第二轮validation loss上升但自由生成指标提高并不矛盾。

因此checkpoint选择不能只看loss，还应运行真实生成，检查：

- 解析率；
- 三字段Accuracy/F1/Recall；
- 非空Think覆盖；
- 输出长度和截断率；
- 混淆矩阵。

### 12.6 训练奖励不能替代外部评测

内部reward上升只表示模型越来越符合当前奖励定义。如果奖励允许两个字段正确、一个关键字段错误时仍获得较高分，模型可能提高reward但没有改善最终安全结论。

因此项目引入三字段联合正确奖励，并坚持使用未参与训练的Banking及冻结ASB进行外部验证。

---

## 13. 个人工作与职责拆解

### 13.1 研究分析

- 分析步级工具调用安全的威胁模型；
- 理解SFT、GRPO、PPO clipping和KL约束；
- 分析安全Recall与任务Utility之间的关系；
- 设计Think监督、奖励和格式约束消融。

### 13.2 数据工程

- 恢复并规范化多来源智能体安全轨迹；
- 设计稳定`source_identity`；
- 实现API Teacher断点续标；
- 建立agreement、conflict和review queue；
- 完成任务级人工裁决；
- 构建SFT JSONL、GRPO Parquet和Validation Manifest；
- 审计训练、验证与外部测试边界。

### 13.3 模型训练

- 完成1.5B LoRA训练与Adapter合并；
- 完成3B全参数BF16 SFT；
- 实现不同Think token权重的加权交叉熵；
- 构建Actor、Reference、Reward和Advantage训练链路；
- 完成真实3B梯度更新门禁。

### 13.4 强化学习算法

- 实现Prompt组内population standardization；
- 处理全等奖励组的零Advantage；
- 实现clipped PPO ratio；
- 实现Reference KL；
- 设计三字段分项奖励与联合正确奖励；
- 分析部分奖励和Reward Hacking问题。

### 13.5 训练工程化

- 实现Tokenizer-aware Token FSM；
- 统一Rollout与LogP重算决策位置；
- 实现完整断点和两代轮换；
- 保存CPU/CUDA RNG及数据游标；
- 实现append-only训练审计日志；
- 实现Clean Validation自动选模；
- 实现无训练信号自动停止；
- 优化单A800显存和磁盘占用。

### 13.6 Agent安全集成

- 定位工具执行前安全检查边界；
- 设计Guardian输入上下文；
- 设计机器可读字段与自然语言反馈；
- 支持危险动作阻断和Agent重新规划；
- 建立安全性与正常任务完成率的联合评测思路。

---

## 14. 推荐简历项目描述

### 14.1 一句话版本

独立构建面向LLM Agent工具调用的步级安全护栏，完成1,656条安全数据治理、Qwen2.5-3B全参数SFT、GRPO及Token-FSM受约束训练系统，在外部Banking评测中实现最高93.1% Accuracy，并将阶段性有害动作Recall由78.6%提升至96.4%。

### 14.2 四条简历Bullet

- 面向恶意请求与间接Prompt Injection驱动的危险工具调用，构建执行前步级Guardian，综合交互历史、当前动作和工具环境输出攻击来源、危害等级与可解释安全反馈。
- 构建1,656条安全轨迹的API Teacher盲标与任务级裁决流水线，识别326条弱标签冲突、修正71行系统性偏差，形成239条SFT、1,208条GRPO和146条独立验证数据。
- 在单张A800上完成Qwen2.5-3B全参数SFT/GRPO闭环，将Banking外部测试由基座22/87可解析提升至87/87，最佳Accuracy/F1/Recall达到93.1%/92.1%/92.1%；阶段性GRPO将有害Recall由78.6%提升至96.4%。
- 自主实现Tokenizer-aware Token FSM与单Actor Constrained GRPO，统一受约束Rollout、Old/New LogP和Reference KL；真实3B门禁实现4/4严格输出、`1e-6`级参数更新及48.3GB峰值显存。

### 14.3 算法岗关键词

```text
LLM Agent Security / Prompt Injection / Step-level Guardrail
Qwen2.5 / Full-parameter SFT / GRPO / PPO Clipping
Group-relative Advantage / Reference KL / Reward Design
Constrained Decoding / Token FSM / Teacher Distillation
```

### 14.4 工程岗关键词

```text
PyTorch / Transformers / FSDP / vLLM / verl / Ray
JSONL / Parquet / Checkpoint Resume / RNG Recovery
Structured Output / Parser / Evaluation Manifest
A800 80GB / BF16 / Memory and Storage Optimization
```

---

## 15. 三分钟面试介绍

我做的是一个面向大模型智能体工具调用安全的步级护栏项目。传统内容审核一般检查用户输入或最终回答，但智能体真正的风险发生在中间工具调用阶段，例如发送邮件、转账、删除文件或者共享敏感数据。如果工具已经执行，再发现风险就太晚了。

因此我把安全护栏放在工具执行器之前。它读取用户原始请求、智能体历史轨迹、工具Observation和当前Action，输出三个安全字段：用户请求是否恶意、当前动作是否受到提示词注入攻击、当前动作的危害等级，同时生成一段证据型解释。这段解释既可以给人工审计，也可以反馈给智能体重新规划。

项目最开始使用1.5B模型和LoRA打通链路，但在外部Banking数据上有31条无法解析，而且一度出现多数类塌缩。我通过预测分布和混淆矩阵发现，训练损失和Accuracy都不足以判断安全能力，所以后续加入跨域AgentDojo数据，并升级到3B全参数SFT。最佳SFT模型在87条Banking数据上实现全部可解析，Accuracy、F1和Recall均超过92%。

随后我发现训练数据中的三个安全字段并不完全可靠，于是设计了API Teacher盲标流程。Teacher看不到旧标签和数据类别，对1,656条轨迹独立判断。最终发现326条冲突。我没有直接覆盖旧标签，而是对同一用户任务执行一致性裁决，最终修正71行，并只保留解释与字段一致的239条SFT样本。

强化学习阶段采用GRPO，同一个Prompt生成多条回答，通过组内相对奖励计算Advantage。第一代GRPO把有害动作Recall从78.57%提升到96.43%，但我同时发现模型学会省略Think，因为奖励只看三个字段。这是一个典型的Reward Hacking：短回答更稳定，也能拿满分。

因此我重新实现了Token级FSM约束生成。固定标签、换行和闭合符由程序保证，模型只生成安全分析、结束分析动作和三个字段值。固定格式进入模型上下文，但不参与策略梯度。Rollout、Old LogP、New LogP和参数更新都由同一个Actor完成，使强化学习真正优化安全语义。

在真实3B单步门禁中，4条Rollout全部严格合法，Old/New概率重算误差为0，梯度成功使模型参数产生约`1e-6`的变化，峰值显存约48GB。目前整个Trainer还支持两代完整断点、随机状态恢复、Clean Validation选模和无训练信号自动停止。

这个项目让我真正理解到，大模型安全训练不能只看最终分数。数据标签、输出协议、奖励函数、概率计算和评测口径中的任何一个问题，都可能产生看似有效、实际无法泛化的提升。

---

## 16. 高频面试问题与回答要点

### 16.1 为什么要做step-level Guard，而不是检查最终回答？

工具调用会产生真实副作用，最终回答出现时动作可能已经执行。Guard必须位于工具调用生成之后、真实执行之前，才能提前阻止风险。

### 16.2 三个字段分别解决什么问题？

用户恶意性描述原始意图，Being Attacked描述当前动作是否受到外部攻击驱动，Harmfulness描述动作本身的后果。拆分后，系统才能选择拒绝用户、忽略攻击或重新规划。

### 16.3 为什么选择GRPO而不是PPO Value Model？

GRPO用同一Prompt下多个回答的相对奖励估计Advantage，不需要额外训练Value Network，能够降低模型数量、显存和训练复杂度，适合个人单卡复现。

### 16.4 Rollout为什么还要重新前向？

Rollout是离散文本，不能直接求导。重新前向是在已知完整Response的条件下计算每个已生成策略Token的Log Probability，不会生成新答案，用于构造可反向传播的策略损失。

### 16.5 同一Prompt的多条Rollout为什么不同？

它们使用相同模型、temperature和top-p，但采样随机数不同。每一步从允许Token的概率分布中随机采样，因此会形成不同Think、结束位置和字段组合。

### 16.6 为什么格式使用FSM，不能让模型自己学？

固定标签没有安全语义，却会制造大量0奖励。FSM保证结构，模型专注判断，能够提高有效奖励密度，并避免策略梯度被格式Token稀释。

### 16.7 固定Token为什么不计算Policy Gradient？

这些Token不是模型在策略空间中选择的动作。它们进入上下文是为了保持后续自回归条件完整，但mask为0，避免模型因确定性标签获得虚假梯度。

### 16.8 为什么第一代GRPO后Think消失？

奖励不要求Think，省略Think既不会扣分，又能减少输出长度和格式失败。模型因此学会了更容易的高奖励策略，属于Reward Hacking。

### 16.9 为什么Validation Loss上升，真实评测反而更好？

Validation Loss使用teacher forcing，衡量正确前文下的token概率；自由生成评测还涉及早期token错误、格式稳定性和序列终止，两者不完全等价。

### 16.10 为什么不能只看Accuracy？

安全数据可能类别不均衡。模型全部预测安全也可能有较高Accuracy，但有害Recall接近0。因此必须同时报告混淆矩阵、逐类Recall、F1和解析失败数量。

### 16.11 GRPO提高Recall但误报增加，如何处理？

这是安全性与Utility权衡。可以通过类别分层采样、奖励权重、决策阈值和反馈后重新规划减少误伤，而不是只追求最高Recall。

### 16.12 为什么完整断点比模型权重大？

完整断点除模型参数外还包括AdamW一阶和二阶动量、scheduler、随机状态和训练进度。3B FP32模型约12GB，加入优化器状态后约36GB是合理的。

---

## 17. 项目亮点总结

该项目可以被概括为五个层次：

1. **安全场景层**：把护栏部署在工具执行之前，解决Agent真实副作用风险；
2. **数据层**：建立API Teacher盲标、冲突队列和任务级裁决，而不是盲信弱标签；
3. **模型层**：完成从1.5B LoRA到3B全参数SFT和GRPO的训练演进；
4. **算法层**：实现多字段奖励、组内相对优势、PPO Clipping、Reference KL和Token-FSM；
5. **工程层**：构建单卡可运行、可恢复、可审计、可自动选模的强化学习Trainer。

项目最有价值的部分并不是机械追逐某个公开分数，而是形成了一套完整的方法论：

```text
先确认数据是否可信
→ 再确认输出协议是否合理
→ 再确认奖励是否对应最终安全目标
→ 再确认梯度是否真正到达模型参数
→ 最后才扩大训练规模和模型规模
```

这套方法能够迁移到企业智能体、浏览器智能体、代码智能体和金融智能体等更多工具调用安全场景。

---

## 18. 给简历整理模型的使用说明

整理为正式简历时，建议遵守以下规则：

1. 项目名称统一使用“AgentSentinel”，不要在项目标题和介绍中引用外部项目名称；
2. 将项目定位为“研究型工程项目”，而不是简单论文复现；
3. 优先突出数据治理、3B全参数训练、Constrained GRPO和工具执行前安全边界；
4. 简历正文保留3至4条Bullet，每条遵循“动作—技术—结果”结构；
5. 93.1% Accuracy是最佳全参数SFT结果；
6. 96.4%有害Recall是第一代GRPO相对其直接父模型的结果，二者不能拼成同一checkpoint；
7. 内部reward相对提升30.2%只能称为训练信号变化，不能写成Accuracy提升30.2%；
8. 可以写“实现Constrained GRPO Trainer并通过真实3B梯度门禁”，不要把单步门禁改写成未经记录的最终模型指标；
9. 可以强调系统具备工具执行前拦截与反馈驱动重新规划能力，不使用没有实际评测支撑的攻击成功率或任务完成率数字；
10. 面试表达应强调个人的分析、设计、实现和取舍，而不是声称所有组件均为从零发明。

